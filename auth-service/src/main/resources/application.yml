spring:
  datasource:
    url: jdbc:postgresql://ecommerce-postgres:5432/authdb
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: validate

    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

    show-sql: true

server:
  address: 0.0.0.0
  port: 8080

jwt:
  secret: mysecretkeymysecretkeymysecretkey12345
